/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['ssl.gstatic.com', 'cataas.com', 'www.google.com', 'picsum.photos', 'lh3.googleusercontent.com'],
  },
};

export default nextConfig;